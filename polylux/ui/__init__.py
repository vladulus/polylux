"""Polylux UI — localhost web UI + tray icon.

Layout:
  state.py   — ServiceState shared between drivers and UI
  server.py  — FastAPI app, REST endpoints
  tray.py    — pystray icon
  templates/ — Jinja2 HTML templates
  static/    — JS, CSS framework files
  themes/    — user-installable theme CSS files
"""
